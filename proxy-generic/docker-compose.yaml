services:
  rest-proxy:
    build:
      context: .
      dockerfile: Dockerfile
    image: rest-proxy:latest
    restart: always
    container_name: rest-proxy-generic
    environment:
      - ANYLOG_NODE_URL=http://66.175.217.145:32349
      - PROXY_PORT=8080
      - MODE=rest
    ports:
      - "8080:8080"
    volumes:
      - ../html:/app/html
